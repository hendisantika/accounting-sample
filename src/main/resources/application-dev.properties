# Dev Profile Configuration
# Use: ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
# Or set SPRING_PROFILES_ACTIVE=dev

# Disable Docker Compose auto-start (manage containers manually)
spring.docker.compose.enabled=false

# Dev-specific logging
logging.level.id.my.hendisantika.accountingsample=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.flywaydb=DEBUG
